[build]
  # No build step – just serve the files in the repo
  command = "echo 'No build step'"
  publish = "."

[functions]
  # This is where your serverless functions live
  directory = "netlify/functions"
